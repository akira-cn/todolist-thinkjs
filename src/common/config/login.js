'use strict';

/**
 * login config
 */
export default {
  type: 'github',
  adapter: {
    github: {
      client_id: '',  //github client id
      client_secret: '',//github client secret
      redirect_uri: ''  //github redirect uri xxx.com/api/user
    }  
  }
}
